# L3Agent Default Configuration
# Minimal configuration with required settings only
# For complete configuration options, see application-reference.properties

# Server configuration
server.port=8080

# LLM provider settings (required)
l3agent.llm.provider=openai
l3agent.llm.api-key=${OPENAI_API_KEY:your_api_key_here}
l3agent.llm.model=gpt-4

# Vector store settings (required)
l3agent.vector-store.engine=hnswlib

# Database settings
l3agent.database.enabled=true

# For detailed configuration options, see:
# - application-reference.properties (complete configuration reference)
# - docs/configuration.md (general configuration documentation)
# - docs/confidence-rating.md (confidence settings documentation)
# - docs/mcp-integration.md (Model Control Plane integration documentation) 